// Archivo de prueba con errores sintácticos intencionales.
// El análisis no se detiene en el primero: los reporta todos.

let a: integer = 10
let b: integer = 20;

if (a < b) {
  print("a es menor");


function saludar(nombre: string) {
  return "Hola " + nombre;
}

class Animal {
  let nombre: string;

  function hablar(): string
    return this.nombre;
  }
}

let resultado = saludar(;
print(resultado);
