// Ejemplo: complejidad media con errores léxicos y sintácticos.
// Se reportan 3 errores léxicos y 2 errores sintácticos.

let saldo: integer = 100

function calcular(n: integer): integer {
  return n * 2;
}

let texto = 'hola';
let total = calcular(3 + ;
print(total);
@
print(saldo);
